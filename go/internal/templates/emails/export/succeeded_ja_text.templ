package export

import "fmt"

// SucceededJaTextは完了したエクスポートの日本語テキストテンプレート
templ SucceededJaText(data Data) {
	エクスポートデータの用意ができました。

{ data.URL }

{ fmt.Sprintf("ダウンロードURLの有効期間はメール送付後から%d時間です。", data.ExpirationHours) }

Wikino
{ data.AppURL }
}
