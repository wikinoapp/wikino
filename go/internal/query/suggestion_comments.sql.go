// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: suggestion_comments.sql

package query

import (
	"context"
	"time"
)

const countSuggestionCommentsBySuggestionID = `-- name: CountSuggestionCommentsBySuggestionID :one
SELECT COUNT(*) FROM suggestion_comments
WHERE suggestion_id = $1 AND space_id = $2
`

type CountSuggestionCommentsBySuggestionIDParams struct {
	SuggestionID string `json:"suggestion_id"`
	SpaceID      string `json:"space_id"`
}

// 編集提案IDでコメント数を取得する
func (q *Queries) CountSuggestionCommentsBySuggestionID(ctx context.Context, arg CountSuggestionCommentsBySuggestionIDParams) (int64, error) {
	row := q.db.QueryRowContext(ctx, countSuggestionCommentsBySuggestionID, arg.SuggestionID, arg.SpaceID)
	var count int64
	err := row.Scan(&count)
	return count, err
}

const createSuggestionComment = `-- name: CreateSuggestionComment :one
INSERT INTO suggestion_comments (space_id, suggestion_id, created_space_member_id, number, body, body_html, created_at, updated_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, space_id, suggestion_id, created_space_member_id, body, body_html, created_at, updated_at, number
`

type CreateSuggestionCommentParams struct {
	SpaceID              string    `json:"space_id"`
	SuggestionID         string    `json:"suggestion_id"`
	CreatedSpaceMemberID string    `json:"created_space_member_id"`
	Number               int32     `json:"number"`
	Body                 string    `json:"body"`
	BodyHtml             string    `json:"body_html"`
	CreatedAt            time.Time `json:"created_at"`
	UpdatedAt            time.Time `json:"updated_at"`
}

// 編集提案コメントを作成する
func (q *Queries) CreateSuggestionComment(ctx context.Context, arg CreateSuggestionCommentParams) (SuggestionComment, error) {
	row := q.db.QueryRowContext(ctx, createSuggestionComment,
		arg.SpaceID,
		arg.SuggestionID,
		arg.CreatedSpaceMemberID,
		arg.Number,
		arg.Body,
		arg.BodyHtml,
		arg.CreatedAt,
		arg.UpdatedAt,
	)
	var i SuggestionComment
	err := row.Scan(
		&i.ID,
		&i.SpaceID,
		&i.SuggestionID,
		&i.CreatedSpaceMemberID,
		&i.Body,
		&i.BodyHtml,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Number,
	)
	return i, err
}

const findSuggestionCommentByID = `-- name: FindSuggestionCommentByID :one
SELECT id, space_id, suggestion_id, created_space_member_id, body, body_html, created_at, updated_at, number FROM suggestion_comments
WHERE id = $1 AND space_id = $2
`

type FindSuggestionCommentByIDParams struct {
	ID      string `json:"id"`
	SpaceID string `json:"space_id"`
}

// IDで編集提案コメントを取得する（スペースIDでスコープ）
func (q *Queries) FindSuggestionCommentByID(ctx context.Context, arg FindSuggestionCommentByIDParams) (SuggestionComment, error) {
	row := q.db.QueryRowContext(ctx, findSuggestionCommentByID, arg.ID, arg.SpaceID)
	var i SuggestionComment
	err := row.Scan(
		&i.ID,
		&i.SpaceID,
		&i.SuggestionID,
		&i.CreatedSpaceMemberID,
		&i.Body,
		&i.BodyHtml,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Number,
	)
	return i, err
}

const findSuggestionCommentByNumber = `-- name: FindSuggestionCommentByNumber :one
SELECT id, space_id, suggestion_id, created_space_member_id, body, body_html, created_at, updated_at, number FROM suggestion_comments
WHERE suggestion_id = $1 AND number = $2 AND space_id = $3
`

type FindSuggestionCommentByNumberParams struct {
	SuggestionID string `json:"suggestion_id"`
	Number       int32  `json:"number"`
	SpaceID      string `json:"space_id"`
}

// 編集提案IDと番号でコメントを取得する（スペースIDでスコープ）
func (q *Queries) FindSuggestionCommentByNumber(ctx context.Context, arg FindSuggestionCommentByNumberParams) (SuggestionComment, error) {
	row := q.db.QueryRowContext(ctx, findSuggestionCommentByNumber, arg.SuggestionID, arg.Number, arg.SpaceID)
	var i SuggestionComment
	err := row.Scan(
		&i.ID,
		&i.SpaceID,
		&i.SuggestionID,
		&i.CreatedSpaceMemberID,
		&i.Body,
		&i.BodyHtml,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Number,
	)
	return i, err
}

const getNextSuggestionCommentNumber = `-- name: GetNextSuggestionCommentNumber :one
SELECT COALESCE(MAX(number), 0) + 1 AS next_number FROM suggestion_comments WHERE suggestion_id = $1
`

// 編集提案内の次のコメント番号を取得する
func (q *Queries) GetNextSuggestionCommentNumber(ctx context.Context, suggestionID string) (int32, error) {
	row := q.db.QueryRowContext(ctx, getNextSuggestionCommentNumber, suggestionID)
	var next_number int32
	err := row.Scan(&next_number)
	return next_number, err
}

const listSuggestionCommentsBySuggestionID = `-- name: ListSuggestionCommentsBySuggestionID :many
SELECT id, space_id, suggestion_id, created_space_member_id, body, body_html, created_at, updated_at, number FROM suggestion_comments
WHERE suggestion_id = $1 AND space_id = $2
ORDER BY created_at ASC
`

type ListSuggestionCommentsBySuggestionIDParams struct {
	SuggestionID string `json:"suggestion_id"`
	SpaceID      string `json:"space_id"`
}

// 編集提案IDでコメント一覧を取得する（作成日時の昇順）
func (q *Queries) ListSuggestionCommentsBySuggestionID(ctx context.Context, arg ListSuggestionCommentsBySuggestionIDParams) ([]SuggestionComment, error) {
	rows, err := q.db.QueryContext(ctx, listSuggestionCommentsBySuggestionID, arg.SuggestionID, arg.SpaceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []SuggestionComment{}
	for rows.Next() {
		var i SuggestionComment
		if err := rows.Scan(
			&i.ID,
			&i.SpaceID,
			&i.SuggestionID,
			&i.CreatedSpaceMemberID,
			&i.Body,
			&i.BodyHtml,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Number,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateSuggestionComment = `-- name: UpdateSuggestionComment :one
UPDATE suggestion_comments
SET body = $2, body_html = $3, updated_at = $4
WHERE id = $1 AND space_id = $5
RETURNING id, space_id, suggestion_id, created_space_member_id, body, body_html, created_at, updated_at, number
`

type UpdateSuggestionCommentParams struct {
	ID        string    `json:"id"`
	Body      string    `json:"body"`
	BodyHtml  string    `json:"body_html"`
	UpdatedAt time.Time `json:"updated_at"`
	SpaceID   string    `json:"space_id"`
}

// 編集提案コメントの本文を更新する（スペースIDでスコープ）
func (q *Queries) UpdateSuggestionComment(ctx context.Context, arg UpdateSuggestionCommentParams) (SuggestionComment, error) {
	row := q.db.QueryRowContext(ctx, updateSuggestionComment,
		arg.ID,
		arg.Body,
		arg.BodyHtml,
		arg.UpdatedAt,
		arg.SpaceID,
	)
	var i SuggestionComment
	err := row.Scan(
		&i.ID,
		&i.SpaceID,
		&i.SuggestionID,
		&i.CreatedSpaceMemberID,
		&i.Body,
		&i.BodyHtml,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Number,
	)
	return i, err
}
