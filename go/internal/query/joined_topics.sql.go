// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: joined_topics.sql

package query

import (
	"context"
)

const listJoinedTopicsByUser = `-- name: ListJoinedTopicsByUser :many
SELECT
  t.id AS topic_id,
  t.number AS topic_number,
  t.name AS topic_name,
  t.visibility AS topic_visibility,
  s.id AS space_id,
  s.identifier AS space_identifier,
  s.name AS space_name
FROM topic_members tm
INNER JOIN topics t ON tm.topic_id = t.id AND t.space_id = tm.space_id
INNER JOIN spaces s ON t.space_id = s.id
INNER JOIN space_members sm ON tm.space_member_id = sm.id AND sm.space_id = tm.space_id
WHERE sm.user_id = $1
  AND sm.active = true
  AND t.discarded_at IS NULL
  AND s.discarded_at IS NULL
ORDER BY tm.last_page_modified_at DESC NULLS LAST, t.number DESC
LIMIT $2
`

type ListJoinedTopicsByUserParams struct {
	UserID string `json:"user_id"`
	Limit  int32  `json:"limit"`
}

type ListJoinedTopicsByUserRow struct {
	TopicID         string `json:"topic_id"`
	TopicNumber     int32  `json:"topic_number"`
	TopicName       string `json:"topic_name"`
	TopicVisibility int32  `json:"topic_visibility"`
	SpaceID         string `json:"space_id"`
	SpaceIdentifier string `json:"space_identifier"`
	SpaceName       string `json:"space_name"`
}

// ユーザーが参加しているトピック一覧を取得する（サイドバー表示用）
// topic_members → topics → spaces を JOIN し、アクティブなスペースメンバーのスペースに限定
func (q *Queries) ListJoinedTopicsByUser(ctx context.Context, arg ListJoinedTopicsByUserParams) ([]ListJoinedTopicsByUserRow, error) {
	rows, err := q.db.QueryContext(ctx, listJoinedTopicsByUser, arg.UserID, arg.Limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListJoinedTopicsByUserRow{}
	for rows.Next() {
		var i ListJoinedTopicsByUserRow
		if err := rows.Scan(
			&i.TopicID,
			&i.TopicNumber,
			&i.TopicName,
			&i.TopicVisibility,
			&i.SpaceID,
			&i.SpaceIdentifier,
			&i.SpaceName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
