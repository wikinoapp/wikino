**/*_test.go
*.log
*.swo
*.swp
.DS_Store
.env
.env.*.local
.env.local
.git
.github
.gitignore
.idea
.pnpm-store/
.vscode
air.toml
bin/
CLAUDE.md
coverage/
docs/
node_modules/
README.md
test-results/
tmp/

# Frontend build artifacts (regenerated by `pnpm build` in the builder stage).
# Keep .gitkeep so the empty directory survives, and keep static/js/vendor/
# since the vendored JS is committed (not produced by the build).
#
# [Ja] フロントエンドのビルド成果物 (builder ステージの `pnpm build` で再生成)。
# .gitkeep は空ディレクトリを保持するために残し、static/js/vendor/ は
# コミット済みのベンダー JS でビルド生成物ではないため残す。
static/css/*
!static/css/.gitkeep
static/js/*
!static/js/vendor/
web/dist

# Committed templ-generated Go files. The builder stage re-runs
# `templ generate`; if committed copies leak into the build context they cause
# duplicate-import errors when merged with the freshly generated files.
#
# [Ja] コミット済みの templ 生成 Go ファイル。builder ステージで
# `templ generate` を再実行するため、ビルドコンテキストに入り込むと
# 新規生成ファイルと混在して import 二重宣言エラーになる。
**/*_templ.go
