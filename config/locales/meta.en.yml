en:
  meta:
    description:
      default: A wiki app that you can find with links.
    title:
      accounts:
        new: Create an account
      email_confirmations:
        edit: Verification
      home:
        show: Home
      topics:
        new: "New topic | %{space_name}"
        show: "%{topic_name} | %{space_name}"
        settings:
          deletions:
            new: "Delete topic | %{topic_name} | %{space_name}"
          general:
            show: "General | %{topic_name} | %{space_name}"
          show: "Settings | %{topic_name} | %{space_name}"
      trash:
        show: Trash
      pages:
        edit: Edit page
        show: "%{page_title} | %{space_name}"
      password_resets:
        new: Forgot your password?
      passwords:
        edit: Change password
      profiles:
        show: "%{name} (@%{atname})"
      settings:
        account:
          deletions:
            new: "Delete account"
        emails:
          show: "Change email"
        show: Settings
        profiles:
          show: "Profile settings"
        two_factor_auth:
          new: "Two-factor authentication"
          show: "Two-factor authentication"
          recovery_codes: "Recovery codes"
      sign_in:
        show: Sign in
      sign_up:
        show: Sign up
      user_sessions:
        two_factor_auth: Two-factor verification
      spaces:
        new: New space
        settings:
          deletions:
            new: "Delete space | %{space_name}"
          exports:
            new: "Export | %{space_name}"
            show: "Export | %{space_name}"
          general:
            show: "General | %{space_name}"
          show: "Settings | %{space_name}"
        show: "%{space_name}"
      welcome:
        show: Find through links.
