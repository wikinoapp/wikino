<nav class="bg-base-200 sticky">
  <div class="hidden lg:block lg:w-[240px]">
    <div class="flex flex-col gap-4 p-4">
      <%= link_to root_path, {
        data: {turbo_action: "advance", turbo_frame: "main-content"}
      } do %>
        <%= render Wikino::UI::Icon.new(name: "logo", size: "24px", class_name: "fill-white") %>
      <% end %>

      <div>
        <input class="input input-bordered" type="text" placeholder="Search...">
      </div>

      <ul class="flex flex-col gap-4">
        <li>
          <%= link_to space_path, {
            class: "flex gap-2 items-center",
            data: {turbo_action: "advance", turbo_frame: "main-content"}
          } do %>
            <%= render Wikino::UI::Icon.new(name: "house", size: "18px", class_name: "fill-white") %>

            <%= t("nouns.home") %>
          <% end %>
        </li>
      </ul>

      <div class="flex flex-col gap-2">
        <div class="font-bold text-xs">
          <%= t("nouns.topics") %>
        </div>

        <div class="flex flex-col gap-2">
          <turbo-frame
            id="joined_topics"
            loading="lazy"
            src="<%= frame_joined_topic_list_path %>"
          ></turbo-frame>

          <%= link_to new_topic_path(viewer!.space_identifier), {
            class: "flex gap-2 items-center",
            data: {turbo_action: "advance", turbo_frame: "main-content"}
          } do %>
            <%= render Wikino::UI::Icon.new(name: "plus", size: "18px", class_name: "fill-white") %>

            <%= t("nouns.new_topic") %>
          <% end %>
        </div>
      </div>

      <hr class="border-stone-600">

      <ul class="flex flex-col gap-4">
        <% if signed_in? %>
          <li>
            <%= button_to session_path(space_identifier: viewer!.space_identifier), {
              class: "flex gap-2 items-center",
              method: :delete
            } do %>
              <%= render Wikino::UI::Icon.new(name: "sign-out", size: "18px", class_name: "fill-white") %>

              <%= t("nouns.sign_out") %>
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to t("nouns.sign_up"), sign_up_path %>
          </li>

          <li>
            <%= link_to t("nouns.sign_in"), sign_in_path %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>

  <div class="hidden sm:max-lg:block sm:max-lg:w-[56px]">
    <div class="flex flex-col gap-4 p-4">
      <%= link_to root_path do %>
        <%= render Wikino::UI::Icon.new(name: "logo", size: "24px", class_name: "fill-white") %>
      <% end %>

      <%= link_to space_path do %>
        <%= render Wikino::UI::Icon.new(name: "house", size: "24px", class_name: "fill-white") %>
      <% end %>
    </div>
  </div>
</nav>
