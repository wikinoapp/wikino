<div class="breadcrumbs py-1 text-sm">
  <ul>
    <li>
      <%= link_to space_entity.name, space_path(space_entity.identifier) %>
    </li>

    <% items.each do |item| %>
      <%= item %>
    <% end %>
  </ul>
</div>
