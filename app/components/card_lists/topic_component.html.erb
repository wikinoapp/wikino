<div class="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3">
  <% topics.each do |topic| %>
    <% if is_guest %>
      <%= render CardLinks::PublicTopicComponent.new(topic:) %>
    <% else %>
      <%= render CardLinks::TopicComponent.new(topic:) %>
    <% end %>
  <% end %>
</div>
