<%= render BaseUI::ContainerComponent.new(
  as: BaseUI::ContainerComponent::As::Main,
  content_screen:,
  options: {
    class: class_names("flex flex-col gap-6 px-4", class_name)
  }
) do %>
  <%= content %>
<% end %>
