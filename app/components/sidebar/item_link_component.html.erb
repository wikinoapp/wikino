<%= link_to path, {
  class: "text-gray-600 hover:bg-gray-100 transition duration-200 ease-in-out p-2 flex items-center",
  title:
} do %>
  <%= render BaseUI::IconComponent.new(
    name: icon_name,
    size: "24px",
    class_name: "fill-gray-600"
  ) %>

  <%= title %>
<% end %>
