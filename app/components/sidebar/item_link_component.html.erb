<%= link_to path, {
  class: "text-gray-700 hover:bg-brand-200 transition duration-200 ease-in-out px-4 py-2 flex gap-2 items-center rounded-md w-full",
  title:
} do %>
  <%= render BaseUI::IconComponent.new(
    name: icon_name,
    size: "22px",
    class_name: "fill-gray-600"
  ) %>

  <%= title %>
<% end %>
