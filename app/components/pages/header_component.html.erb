<div
  class="bg-brand-100 z-sticky-bar group sticky top-0 min-h-[76px] md:min-h-[49px]"
  data-controller="stuck"
>
  <%# スクロールする前に表示されるヘッダー %>
  <div class="block group-data-stuck:hidden">
    <%= render Headers::MainTitleComponent.new(title: page.display_title) do |main_title| %>
      <%= main_title.with_actions do %>
        <%= render Pages::HeaderActionsComponent.new(signed_in:, page:) %>
      <% end %>
    <% end %>
  </div>

  <%# スクロールした後に表示されるヘッダー %>
  <div class="hidden group-data-stuck:block border-b-1 border-gray-300">
    <%= render Pages::StickyHeaderComponent.new(signed_in:, page:) %>
  </div>
</div>
