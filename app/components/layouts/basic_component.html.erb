<div class="flex flex-col gap-4 bg-brand-100">
  <%= header %>

  <%= main %>

  <% if footer? %>
    <%= footer %>
  <% end %>
</div>

<%= render FixedBottomComponent.new(current_page_name:, current_user:) %>
