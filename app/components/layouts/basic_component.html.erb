<div class="bg-brand-100 flex min-h-screen flex-col gap-4">
  <%= header %>

  <%= main %>

  <% if footer? %>
    <%= footer %>
  <% end %>
</div>

<%= render FixedBottomComponent.new(current_page_name:, current_user:) %>
