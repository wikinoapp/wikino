<%= link_to path, {
  class: "text-gray-600 hover:text-gray-950 text-sm transition duration-200 ease-in-out group",
  title:
} do %>
  <%= render BaseUI::IconComponent.new(
    name: icon_name,
    size: "2rem",
    class_name: "fill-gray-600 group-hover:fill-gray-950 transition duration-200 ease-in-out"
  ) %>
<% end %>
