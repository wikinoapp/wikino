mutation (
  $id: ID!
  $body: String!
) {
  updateNote(
    input: {
      id: $id,
      body: $body
    }
  ) {
    note {
      bodyHtml
      updatedAt
    }

    errors {
      message
    }
  }
}
