<div class="flex w-fit items-center gap-4 rounded-full border border-gray-400 bg-base-300 px-4 py-1 sm:hidden">
  <%= link_to space_path(Current.space!.identifier), class: "flex flex-col gap-1 items-center" do %>
    <%= render Basic::IconComponent.new(name: "house", size: "26px", class_name: "fill-base-content") %>

    <div class="text-xs text-gray-500">
      <%= t("nouns.home") %>
    </div>
  <% end %>
</div>
