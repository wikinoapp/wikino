<div <%= tag.attributes(class: class_name) %>>
  <div class="sticky top-0 z-navbar hidden lg:block">
    <%= render Navbars::TopComponent.new(current_page_name:) %>
  </div>

  <div class="flex flex-col gap-4">
    <div class="mx-auto w-full max-w-screen-lg">
      <%= content %>
    </div>

    <% unless Current.viewer.signed_in? %>
      <%= render Footers::PublicComponent.new %>
    <% end %>
  </div>

  <div class="z-50 fixed bottom-0 left-1/2 flex translate-x-[-50%] flex-col gap-4 p-4">
    <%= render Toasts::FlashComponent.new(flash:) %>
    <%= render Navbars::BottomComponent.new(current_page_name:) %>
  </div>
</div>
