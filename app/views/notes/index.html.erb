<% content_for :page_title, "Nonoto" %>

<h1>Hi, <%= current_user.email %>!</h1>

<ul>
  <% @note_entities.each do |note_entity| %>
    <li>
      <%= link_to edit_note_path(note_entity.database_id) do %>
        <%= note_entity.title %>
      <% end %>
    </li>
  <% end %>
</ul>
