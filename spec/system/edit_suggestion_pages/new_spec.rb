# typed: false
# frozen_string_literal: true

require "rails_helper"

RSpec.describe "EditSuggestionPages::New", type: :system do
  # 実際のユースケースでは、ユーザーは必ずページ編集画面から編集提案フォームにアクセスします。
  # そのため、ページ編集画面経由でのテストのみを残します。
end