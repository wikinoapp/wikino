# プロジェクト間ファイル同期 2026/02/27

## 概要

Wikino / Annict / Mewst の 3 プロジェクト間で共有ファイルの差分を調査し、同期する。

## タスクリスト

### フェーズ 1: 採用方針の決定

- [x] **1-1**: 以下の差分レポートを確認し、各差分の採用方針を記入する
  - 各差分の「採用方針」チェックボックスにチェックを入れる
  - 必要に応じて「その他」に補足を記入する
  - **重要**: 「スキップ（対応不要）」が選ばれた差分は `.claude/commands/sync.md` の「スキップ済みの差分」セクションに追記するタスクも含めること
  - 「スキップ（あとで対応）」が選ばれた差分は別途作業計画書を作成するタスクを追加すること
  - 他プロジェクトへの反映は、各プロジェクトで `/sync` を実行して対応する
  - **担当**: ユーザー

### フェーズ 1.5: 採用方針に基づく対応

- [x] **1.5-1**: 差分 2 — `docs/reviews/template.md` を Annict/Mewst の方式に更新
- [x] **1.5-2**: 差分 3 — `rails/docs/` ディレクトリ作成の作業計画書を作成（`docs/plans/2_todo/rails-docs-directory.md`）
- [x] **1.5-3**: 差分 4 — golangci-lint を v2.7.2 に更新（`go/CLAUDE.md` + `.github/workflows/go-ci.yml`）
- [x] **1.5-4**: 差分 8 — `Dockerfile.dev` に `touch ~/.zshrc` を追加（USER 切替後）
- [x] **1.5-5**: スキップ済み差分リストの更新（`.claude/commands/sync.md` に 16 件追記）

### スキップ済み差分のクリーンアップ結果

以下のスキップ済み差分を確認した結果、いずれも解消されていない（そのまま維持）：

- `rails/.rspec`: require 対象の違い — 依然として Wikino/Mewst は `rails_helper`、Annict は `spec_helper`
- `Dockerfile.dev`: Mewst の imagemagick 欠落 — 依然として Mewst に imagemagick なし

### フェーズ 2: 差分レポート

---

#### 差分 1: `.claude/commands/sync.md` の複数改善が Annict/Mewst に未反映

**対象ファイル**: `.claude/commands/sync.md`

**差分の内容**:

- **Wikino**: 同日複数回実行対応の連番命名（`sync-YYYYMMDD-{3桁連番}.md`）、削除セクションに「設計」追加、Task tool の `subagent_type: Explore` 指定、スキップ済み差分の解消確認指示、差分レポートをタスクリストに統合する構造、「スキップ（あとで対応）」選択肢、推奨理由フィールドの明示
- **Annict**: 上記改善がすべて未反映。連番なし、`subagent_type: general-purpose`、差分レポートと設計セクションが分離
- **Mewst**: Annict と同様、未反映

**採用方針**:

- [x] Wikino の方式を採用 (Recommended)
- [ ] Annict/Mewst の方式を採用
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Wikino の改善はワークフローの使い勝手と追跡性を向上させる実質的な改善であり、Annict/Mewst に反映すべき。

---

#### 差分 2: `docs/reviews/template.md` の例セクション見出しが Wikino で古い

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Wikino**: 見出しが `（例: 修正方針が明確な場合）`、参照が `@CLAUDE.md#ログ出力`
- **Annict**: 見出しが `（例: 修正方針が明確な場合でも「対応方針」「回答」は必須）`、参照が `@go/CLAUDE.md#ログ出力`
- **Mewst**: Annict と同一

**採用方針**:

- [ ] Wikino の方式を採用
- [x] Annict/Mewst の方式を採用 (Recommended)
- [ ] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Annict/Mewst の見出しは「対応方針」「回答」が必須であることを明示しており、レビューガイドラインとしてより正確。参照先も `@go/CLAUDE.md` がより適切。

---

#### 差分 3: `docs/plans/template.md` に Annict のみ Rails 版ガイドラインリンク追加

**対象ファイル**: `docs/plans/template.md`

**差分の内容**:

- **Wikino**: Rails 版ガイドラインは `@rails/CLAUDE.md` のみ
- **Annict**: `@rails/CLAUDE.md` に加えて `@rails/docs/architecture-guide.md`、`@rails/docs/testing-guide.md`、`@rails/docs/security-guide.md` の 3 リンクを追加
- **Mewst**: Wikino と同一（`@rails/CLAUDE.md` のみ）

**採用方針**:

- [ ] Wikino/Mewst の方式を採用
- [ ] Annict の方式を採用 (Recommended)
- [ ] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [x] その他: 現状Wikinoには `rails/docs/` ディレクトリは存在しませんが、`rails/CLAUDE.md` の内容を別ファイルに切り出し、
      `rails/docs/` ディレクトリを作成してそこに配置すると良さそうに思いました。その対応をするための作業計画書の作成をお願いします

**推奨理由**: ガイドラインリンクが充実している方が作業計画書作成時に参照しやすい。ただし Wikino/Mewst に該当ドキュメントが存在するかの確認が必要。

---

#### 差分 4: `go/CLAUDE.md` と `.github/workflows/go-ci.yml` の golangci-lint バージョン不一致

**対象ファイル**: `go/CLAUDE.md`, `.github/workflows/go-ci.yml`

**差分の内容**:

- **Wikino**: golangci-lint `v2.6.2`（CLAUDE.md と CI 両方）
- **Annict**: golangci-lint `v2.7.2`（CLAUDE.md と CI 両方）
- **Mewst**: CLAUDE.md は `v2.6.2`、CI は `v2.7.2`（不整合あり）

**採用方針**:

- [ ] Wikino の方式を採用（v2.6.2）
- [x] Annict の方式を採用（v2.7.2）(Recommended)
- [ ] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: v2.7.2 が最新バージョンであり、Annict/Mewst の CI で既に使用されている。Wikino と Mewst の CLAUDE.md を更新すべき。

---

#### 差分 5: `go/docs/templ-guide.md` と `go/docs/validation-guide.md` の Annict テストコメントが古い

**対象ファイル**: `go/docs/templ-guide.md`, `go/docs/validation-guide.md`

**差分の内容**:

- **Wikino**: テストコメントが「共有DB接続プールからトランザクションをセットアップ」
- **Annict**: テストコメントが「テストDBとトランザクションをセットアップ」（古い記述）
- **Mewst**: Wikino と同一

**採用方針**:

- [x] Wikino/Mewst の方式を採用 (Recommended)
- [ ] Annict の方式を採用
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Wikino/Mewst の記述が TestMain パターンの実態をより正確に反映している。

---

#### 差分 6: `go/CLAUDE.md` の SetupTx/GetTestDB 使い分けテーブルが Annict に欠落

**対象ファイル**: `go/CLAUDE.md`

**差分の内容**:

- **Wikino**: `SetupTx` と `GetTestDB` の使い分けを示す比較テーブルあり
- **Annict**: テーブルなし
- **Mewst**: Wikino と同一（テーブルあり）

**採用方針**:

- [x] Wikino/Mewst の方式を採用 (Recommended)
- [ ] Annict の方式を採用
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: 使い分けテーブルはテスト作成時の判断を助ける有用な情報であり、Annict にも追加すべき。

---

#### 差分 7: `Dockerfile.dev` に Annict で hivemind が欠落

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Wikino**: hivemind `v1.1.0` インストール済み
- **Annict**: hivemind なし
- **Mewst**: hivemind `v1.1.0` インストール済み

**採用方針**:

- [x] Wikino/Mewst の方式を採用 (Recommended)
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: hivemind は開発環境の Procfile 実行に使用されるツール。Annict でも必要であれば追加すべき。

---

#### 差分 8: `Dockerfile.dev` の `touch ~/.zshrc` の有無と位置の不一致

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Wikino**: `touch ~/.zshrc` なし
- **Annict**: `USER ${USERNAME}` 後に `touch ~/.zshrc` あり
- **Mewst**: `useradd` 直後（USER 切替前）に `touch ~/.zshrc` あり

**採用方針**:

- [x] Annict の方式を採用（USER 切替後に touch）(Recommended)
- [ ] Mewst の方式を採用（useradd 直後に touch）
- [ ] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: zsh 起動時に `.zshrc` が存在しないと警告が出る場合がある。Annict の方式（USER 切替後に実行）が正しいホームディレクトリに作成される点で安全。

---

#### 差分 9: `Dockerfile.dev` の Bundler バージョン不一致

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Wikino**: Bundler `2.5.16`
- **Annict**: Bundler `2.3.12`
- **Mewst**: Bundler `2.5.9`

**採用方針**:

- [ ] Wikino の方式を採用（2.5.16）(Recommended)
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Wikino が最新バージョンを使用している。各プロジェクトで Gemfile.lock との整合性を確認した上で更新すべき。

---

#### 差分 10: `go/.golangci.yml` の depguard ルール差異

**対象ファイル**: `go/.golangci.yml`

**差分の内容**:

- **Wikino**: `policy-layer`、`markup-layer`、`worker-layer` の depguard ルールが追加されている
- **Annict**: 上記 3 層なし。代わりに seed 関連ファイルの除外ルールあり。validator ルールのコメントが異なる
- **Mewst**: Wikino と同様に 3 層なし。seed 除外ルールもなし

**採用方針**:

- [ ] スキップ（対応不要）(Recommended)
- [ ] スキップ（あとで対応）
- [x] その他: 「スキップ（対応不要）」ですが、「スキップ済みの差分」セクションには `policy-layer`、`markup-layer` のみ記載するようにしてください
      `worker-layer` は今後どのプロジェクトにも生まれると思います

**推奨理由**: depguard の層は各プロジェクトのアーキテクチャに依存する。Wikino の policy/markup/worker は Wikino 固有の機能、Annict の seed 除外は Annict 固有の機能であるため、プロジェクト固有の差異として扱うのが適切。

---

#### 差分 11: `CLAUDE.md` の CI セクションで Annict に `fmt-ci.yml` 記載なし

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Wikino**: CI 設定に `go-ci.yml`、`fmt-ci.yml`、`rails-ci.yml` の 3 つを記載
- **Annict**: `go-ci.yml`、`rails-ci.yml` の 2 つのみ（`fmt-ci.yml` なし）
- **Mewst**: Wikino と同一（3 つ記載）

**採用方針**:

- [x] Wikino/Mewst の方式を採用
- [x] スキップ（対応不要）(Recommended)
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Annict に `fmt-ci.yml` が存在しない可能性がある（Annict 側で Oxfmt を導入していない）。各プロジェクトの実際の CI ファイル構成に合わせるべきであり、一律の同期は不適切。

---

#### 差分 12: `CLAUDE.md` の docs/ 管理セクションが Annict/Mewst に欠落

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Wikino**: 仕様書（`docs/specs/`）への参照セクション、レビュー時に参照するガイドラインセクションあり
- **Annict**: なし
- **Mewst**: なし

**採用方針**:

- [x] Wikino の方式を採用 (Recommended)
- [x] スキップ（対応不要）
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: docs/ 管理は 3 プロジェクト共通の仕組み（`docs/README.md` は同一）のため、CLAUDE.md にも同様のセクションがあるべき。

---

#### 差分 13: `go/CLAUDE.md` の Worker/ドメインID型/スペースIDセクション（Wikino 固有）

**対象ファイル**: `go/CLAUDE.md`

**差分の内容**:

- **Wikino**: Worker/river セクション、ドメイン ID 型セクション、スペース ID によるクエリスコープ（セキュリティ）セクションが追加されている
- **Annict**: なし
- **Mewst**: なし

**採用方針**:

- [x] スキップ（対応不要）(Recommended)
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Worker/ドメインID型/スペースIDスコープは Wikino 固有のドメイン機能。Annict/Mewst でも同等の機能が必要になった場合に個別に追加すべき。

---

#### 差分 14: `go/docs/architecture-guide.md` の Mewst 固有の問題

**対象ファイル**: `go/docs/architecture-guide.md`

**差分の内容**:

- **Wikino**: Presentation 層ヘルパーに `image`, `i18n`, `session` を記載
- **Annict**: Wikino と同一
- **Mewst**: `image` が欠落（`i18n`, `session` のみ）。また、コード例に Annict 固有の `UserCalendar` モデルが残存

**採用方針**:

- [x] スキップ（対応不要）(Recommended)
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Mewst に画像機能がないため `image` の欠落は意図的と推測される。UserCalendar の残存は Mewst 側で `/sync` 実行時に対応すべき Mewst 固有の問題。

---

#### 差分 15: `rails/eslint.config.mjs` の globalIgnores 差異

**対象ファイル**: `rails/eslint.config.mjs`

**差分の内容**:

- **Wikino**: `globalIgnores` に `app/assets/builds/**/*` と `tailwind.config.js` のみ
- **Annict**: 上記に加えて `coverage/**/*`, `public/**/*`, `vendor/**/*`, `tmp/**/*`, `log/**/*`, `node_modules/**/*` を追加。また `globals.node` と `$: "readonly"` も追加
- **Mewst**: Wikino と同一

**採用方針**:

- [ ] Wikino/Mewst の方式を採用
- [ ] Annict の方式を採用
- [x] スキップ（対応不要）(Recommended)
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Annict の追加 ignores（`coverage/`, `public/` 等）と `$: "readonly"`（jQuery）は Annict 固有の事情を反映している。`node_modules/**/*` は ESLint がデフォルトで除外するため不要。

---

#### 差分 16: `go/docs/security-guide.md` にスペースIDクエリスコープセクション（Wikino 固有）

**対象ファイル**: `go/docs/security-guide.md`

**差分の内容**:

- **Wikino**: 「スペースIDによるクエリスコープ」セクションとチェックリスト項目あり
- **Annict**: なし
- **Mewst**: なし

**採用方針**:

- [x] スキップ（対応不要）(Recommended)
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: スペースは Wikino 固有のドメイン概念であり、セキュリティガイドのこの部分はプロジェクト固有。

---

#### 差分 17: `.github/workflows/go-ci.yml` の Go/PostgreSQL バージョン差異

**対象ファイル**: `.github/workflows/go-ci.yml`

**差分の内容**:

- **Wikino**: Go `1.25.5`, PostgreSQL `18.1`
- **Annict**: Go `1.25.1`, PostgreSQL `17.5`
- **Mewst**: Go `1.25.4`, PostgreSQL `16.2`

**採用方針**:

- [x] スキップ（対応不要）(Recommended)
- [ ] スキップ（あとで対応）
- [ ] その他: <!-- 記入 -->

**推奨理由**: Go と PostgreSQL のバージョンは各プロジェクトの本番環境に依存するため、一律の同期は不適切。各プロジェクトで個別にアップグレードすべき。
