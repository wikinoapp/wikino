# プロジェクト間ファイル同期 2026/02/24

## 概要

Wikino / Annict / Mewst の 3 プロジェクト間でガイドライン・設定ファイルを比較し、差分を同期する。

## 設計

### 差分 1: `CLAUDE.md` - 標準ファイル名の種類数

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Wikino**: ハンドラーの標準ファイル名を「8種類のみ」と記載
- **Annict**: 「9種類のみ」と記載
- **Mewst**: 「9種類のみ」と記載

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Annict/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 2: `CLAUDE.md` - コミット前チェックの参照範囲

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Wikino**: 「Rails 版の具体的なコマンドは `rails/CLAUDE.md` を参照」のみ（Go 版への参照なし）
- **Annict**: Go 版と Rails 版の両方のセクション参照を明示
- **Mewst**: Go 版と Rails 版の両方のセクション参照を明示

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Annict/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 3: `go/CLAUDE.md` - テストの「新規テスト作成時のガイドライン」セクション

**対象ファイル**: `go/CLAUDE.md`

**差分の内容**:

- **Wikino**: 「新規テスト作成時のガイドライン」セクションあり（`SetupTx` と `GetTestDB` の使い分け表を含む）
- **Annict**: なし
- **Mewst**: なし

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 4: `go/docs/architecture-guide.md` - Middleware の templates 依存ポリシー

**対象ファイル**: `go/docs/architecture-guide.md`, `go/.golangci.yml`

**差分の内容**:

- **Wikino**: Middleware から templates への依存を**許可**（エラーページやメンテナンスページのレンダリングのため）。golangci.yml の depguard でも許可。
- **Annict**: Middleware から templates への依存を**禁止**。golangci.yml の depguard でも禁止。
- **Mewst**: Middleware から templates への依存を**禁止**。golangci.yml の depguard でも禁止。

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] Annict/Mewst の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 5: `go/.golangci.yml` - RateLimit 層の deny ルール

**対象ファイル**: `go/.golangci.yml`

**差分の内容**:

- **Wikino**: RateLimit 層は query のみ禁止。ファイルパターンは `**/internal/ratelimit/**`
- **Annict**: RateLimit 層は query, usecase, handler, middleware, viewmodel, templates を禁止。ファイルパターンは `**/internal/ratelimit/*.go`
- **Mewst**: Annict と同じ

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Annict/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 6: `go/.golangci.yml` - Validator 層のコメント

**対象ファイル**: `go/.golangci.yml`

**差分の内容**:

- **Wikino**: `バリデーションはHandler層で行います。`
- **Annict**: `ValidatorはUseCaseに依存できません。`
- **Mewst**: `ValidatorはUseCaseに依存できません。`

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Annict/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 7: `go/Makefile` - tparse インストール方法

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Wikino**: `go install github.com/mfridman/tparse`（go.mod のバージョンを使用）
- **Annict**: `go install github.com/mfridman/tparse@latest`
- **Mewst**: Wikino と同じ（go.mod のバージョンを使用）

**採用方針**:

- [x] Wikino/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 8: `go/Makefile` - test-pkg/test-run の tparse オプション

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Wikino**: `tparse`（オプションなし）
- **Annict**: `tparse -all`（全テスト結果を表示）
- **Mewst**: Wikino と同じ（オプションなし）

**採用方針**:

- [ ] Wikino/Mewst の方式を採用（他プロジェクトに反映）
- [x] Annict の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 9: `.github/workflows/go-ci.yml` - Lint ステップの goimports チェック構成

**対象ファイル**: `.github/workflows/go-ci.yml`

**差分の内容**:

- **Wikino**: `make templ-generate` + `make goimports` + `git diff` を1ステップで実行
- **Annict**: Wikino と同じ（コメント付き）
- **Mewst**: `make templ-generate` + `git diff` と `make goimports-check` を別ステップに分離

**採用方針**:

- [ ] Wikino/Annict の方式を採用（他プロジェクトに反映）
- [x] Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 10: `.github/workflows/go-ci.yml` - テストステップの `CI: true` 環境変数

**対象ファイル**: `.github/workflows/go-ci.yml`

**差分の内容**:

- **Wikino**: テストステップに `CI: true` 環境変数なし
- **Annict**: テストステップに `CI: true` 環境変数あり
- **Mewst**: テストステップに `CI: true` 環境変数あり

**採用方針**:

- [x] Annict/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 11: `rails/Makefile` - テストコマンドの環境変数管理

**対象ファイル**: `rails/Makefile`

**差分の内容**:

- **Wikino**: `OP_RUN_TEST` 変数（`APP_ENV=test op run ...`）を使用してテストを実行
- **Annict**: `RAILS_ENV=test` を直接指定してテスト実行（op run を使わない）
- **Mewst**: Wikino と同じ

**採用方針**:

- [x] Wikino/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 12: `.claude/commands/commit.md` - 「変更が多岐にわたる場合」の配置

**対象ファイル**: `.claude/commands/commit.md`

**差分の内容**:

- **Wikino**: ステップ 4 の外側（「原則」セクションの後）に配置
- **Annict**: ステップ 4 のサブ項目として配置
- **Mewst**: Annict と同じ

**採用方針**:

- [x] Annict/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 13: `.claude/commands/review.md` - 引数の例示パス

**対象ファイル**: `.claude/commands/review.md`

**差分の内容**:

- **Wikino**: `/review main docs/designs/suspend-user.md`（`docs/designs/` パス）
- **Annict**: Wikino と同じ
- **Mewst**: `/review main docs/plans/1_doing/feature.md`（`docs/plans/1_doing/` パス）

Mewst の方が実際のディレクトリ構造（`docs/plans/`）と一致している。

**採用方針**:

- [x] Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 14: `.claude/commands/review.md` - レビュードキュメントの命名規則

**対象ファイル**: `.claude/commands/review.md`

**差分の内容**:

- **Wikino**: `{ブランチ名}.md`。同名ファイル存在時は末尾に連番（例: `rate-limit-1.md`）
- **Annict**: Wikino と同じ
- **Mewst**: `{ブランチ名}-{3桁連番}.md`。`done/` ディレクトリも検索して番号重複を回避（例: `sign-up-1-1-001.md`）

Mewst の方式はゼロパディング 3 桁で一貫性があり、`done/` も検索するため番号衝突を防げる。

**採用方針**:

- [x] Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] Wikino/Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 15: `docs/plans/template.md` - 「公開時の注意事項」の有無

**対象ファイル**: `docs/plans/template.md`, `docs/specs/template.md`

**差分の内容**:

- **Wikino**: 「公開時の注意事項」コメントなし
- **Annict**: あり（開発用ドメイン名は `example.dev` を使用、環境変数はサンプル値のみ）
- **Mewst**: Annict と同じ

**採用方針**:

- [x] Annict/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 16: `docs/plans/template.md` - 「実装ガイドラインの参照」コメントの表現

**対象ファイル**: `docs/plans/template.md`

**差分の内容**:

- **Wikino**: 「設計を行う前に」「設計の段階で」と記述
- **Annict**: 「作業計画書を作成する前に」「作業計画書作成の段階で」と記述
- **Mewst**: Annict と同じ

**採用方針**:

- [x] Annict/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 17: `docs/plans/template.md` - 設計セクション後のプレースホルダー

**対象ファイル**: `docs/plans/template.md`

**差分の内容**:

- **Wikino**: 設計セクションのコメント閉じタグ後にプレースホルダーなし
- **Annict**: `[必要に応じて設計の詳細を記述]` あり
- **Mewst**: Annict と同じ

**採用方針**:

- [x] Annict/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 18: `docs/plans/template.md` - 標準ファイル数の記述

**対象ファイル**: `docs/plans/template.md`

**差分の内容**:

- **Wikino**: 「**ファイル名は標準の8種類のみ**」
- **Annict**: 「**ファイル名は標準の8種類のみ**」
- **Mewst**: 「**ファイル名は標準の9種類のみ**」

差分 1 と連動する内容。

**採用方針**:

- [x] 差分 1 の決定に合わせる (Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 19: `docs/reviews/template.md` - 用語「設計書」vs「作業計画書」

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Wikino**: レビュー情報テーブルに「作業計画書（指定があれば）」
- **Annict**: 「設計書（指定があれば）」
- **Mewst**: Wikino と同じ

**採用方針**:

- [x] Wikino/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 20: `docs/reviews/template.md` - 問題点フォーマットの説明方式

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Wikino**: 番号付きリストで「3つの項目（修正案・対応方針・回答）を含める」方式
- **Annict**: 箇条書きで「修正方針が明確な場合」「判断が必要な場合」の2パターンで説明
- **Mewst**: Wikino と同じ

**採用方針**:

- [x] Wikino/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 21: `docs/reviews/template.md` - 「設計改善の提案」セクション

**対象ファイル**: `docs/reviews/template.md`

**差分の内容**:

- **Wikino**: 「設計改善の提案」セクションあり
- **Annict**: なし
- **Mewst**: あり

**採用方針**:

- [x] Wikino/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 22: `go/docs/templ-guide.md` - Flash コンポーネントの引数

**対象ファイル**: `go/docs/templ-guide.md`

**差分の内容**:

- **Wikino**: `@components.Flash(flash)`
- **Annict**: Wikino と同じ
- **Mewst**: `@components.Flash(ctx, flash)`

**採用方針**:

- [ ] スキップ（対応不要）(Recommended)
- [x] その他: Mewstの設定を変えるので、ここでは何もしなくて良いです

---

### 差分 23: `Dockerfile.dev` - golangci-lint バージョン

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Wikino**: v2.7.2
- **Annict**: v2.6.2
- **Mewst**: v2.6.2

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 24: `Dockerfile.dev` - mise shims 方式の説明コメント

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Wikino**: `mise reshim` の前に shims 方式を使う理由の説明コメント（3行）あり
- **Annict**: コメントなし
- **Mewst**: Wikino と同じ

**採用方針**:

- [x] Wikino/Mewst の方式を採用（他プロジェクトに反映）(Recommended)
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### 差分 25: `Dockerfile.dev` - Mewst の imagemagick 欠落

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Wikino**: `imagemagick` パッケージあり
- **Annict**: あり
- **Mewst**: なし

**採用方針**:

- [ ] Wikino/Annict の方式を採用（他プロジェクトに反映）(Recommended)
- [x] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

## タスクリスト

### フェーズ 1: 採用方針の決定

- [x] **1-1**: 差分レポートを確認し、各差分の採用方針を記入する
  - 各差分の「採用方針」チェックボックスにチェックを入れる
  - 必要に応じて「その他」に補足を記入する
  - **担当**: ユーザー

### フェーズ 2: タスクリストの作成

- [x] **2-1**: 採用方針を元に自プロジェクトのタスクリストを追加する
  - ユーザーが記入した採用方針に基づき、フェーズ 3 以降に自プロジェクトの具体的なタスクを追加する
  - **重要**: 「スキップ（対応不要）」が選ばれた差分は `.claude/commands/sync.md` の「スキップ済みの差分」セクションに追記するタスクも含めること
  - 他プロジェクトへの反映は、各プロジェクトで `/sync` を実行して対応する
  - **担当**: Claude

### フェーズ 3: ガイドラインの更新

- [x] **3-1**: `CLAUDE.md` の標準ファイル名の種類数とコミット前チェック参照範囲を更新する（差分 1, 2）
  - 「標準ファイル名（8種類のみ）」→「標準ファイル名（9種類のみ）」に変更
  - 「コミット前のチェック」セクションに Go 版のセクション参照を追加
  - **担当**: Claude

- [x] **3-2**: `go/.golangci.yml` の RateLimit 層の deny ルールと Validator 層のコメントを更新する（差分 5, 6）
  - RateLimit 層: ファイルパターンを `**/internal/ratelimit/*.go` に変更し、query, usecase, handler, middleware, viewmodel, templates を禁止
  - Validator 層: コメントを「ValidatorはUseCaseに依存できません。」に変更
  - **担当**: Claude

### フェーズ 4: ビルド・CI 設定の更新

- [x] **4-1**: `go/Makefile` の test-pkg/test-run に `tparse -all` オプションを追加する（差分 8）
  - test-pkg の tparse → tparse -all
  - test-run の tparse → tparse -all
  - **担当**: Claude

- [x] **4-2**: `.github/workflows/go-ci.yml` の goimports チェック分離と CI 環境変数を追加する（差分 9, 10）
  - Lint ステップ: `make templ-generate` + `git diff` と `make goimports-check` を別ステップに分離
  - Test ステップ: `CI: true` 環境変数を追加
  - **担当**: Claude

### フェーズ 5: Claude Code 設定の更新

- [x] **5-1**: `.claude/commands/commit.md` の「変更が多岐にわたる場合」をステップ 4 のサブ項目に移動する（差分 12）
  - 「原則」セクション後の独立した文 → ステップ 4 のサブ項目として配置
  - **担当**: Claude

- [x] **5-2**: `.claude/commands/review.md` の引数例示パスとレビュードキュメント命名規則を更新する（差分 13, 14）
  - 引数例示パス: `docs/designs/suspend-user.md` → `docs/plans/1_doing/feature.md`
  - レビュードキュメント命名規則: `{ブランチ名}-{3桁連番}.md` 方式に変更し、`done/` も検索して番号重複を回避
  - **担当**: Claude

### フェーズ 6: ドキュメントテンプレートの更新

- [x] **6-1**: `docs/plans/template.md` を更新する（差分 15, 16, 17, 18）
  - 「公開時の注意事項」コメントを追加
  - 「設計を行う前に」→「作業計画書を作成する前に」、「設計の段階で」→「作業計画書作成の段階で」に変更
  - 設計セクション後に `[必要に応じて設計の詳細を記述]` プレースホルダーを追加
  - 標準ファイル数を「8種類」→「9種類」に変更
  - **担当**: Claude

### フェーズ 7: スキップ済み差分の記録

- [x] **7-1**: `.claude/commands/sync.md` の「スキップ済みの差分」セクションにスキップした差分を追記する
  - `go/docs/templ-guide.md`: Flash コンポーネントの引数の違い（Mewst 側の設定を変更するため対応不要）
  - `Dockerfile.dev`: Mewst の imagemagick 欠落（プロジェクト固有のため）
  - **担当**: Claude
