# プロジェクト間ファイル同期 2026/02/24

## 概要

Wikino・Annict・Mewst の 3 プロジェクト間で共通ファイルの差分を調査し、同期を行う。

## 設計

### カテゴリ 1: ガイドライン・ドキュメント

#### 差分 1: CLAUDE.md - セッションストアセクションの有無

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Wikino**: セッションストア（PostgreSQL）の詳細セクションがない
- **Annict**: セッションストア（PostgreSQL）の詳細セクションがある
- **Mewst**: セッションストア（PostgreSQL）の詳細セクションがある

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 2: CLAUDE.md - CI/CD 設定ファイルの記載

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Wikino**: `lint-and-test.yml`（Rails 版のみ）を記載
- **Annict**: `go-ci.yml` + `rails-ci.yml`（Go 版も含む）を記載
- **Mewst**: `go-ci.yml` + `rails-ci.yml`（Go 版も含む）を記載

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 3: CLAUDE.md - レビューガイドライン内の用語

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Wikino**: 「リクエストバリデーション」「ビューモデル・ユースケース・ワーカー」「Usecase、Worker、Repository」「Request DTOパターン」
- **Annict/Mewst**: 「バリデーション方針」「ビューモデル・ユースケース」「Usecase、Repository」「バリデーションの分類」

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 4: CLAUDE.md - ハンドラー標準ファイル名数

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Wikino/Annict**: 「8種類のみ」と記載
- **Mewst**: 「9種類のみ」と記載

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Mewst の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 5: CLAUDE.md - Rails CI に prettier を含むかどうか

**対象ファイル**: `CLAUDE.md`

**差分の内容**:

- **Wikino/Mewst**: Rails CI の説明に prettier を含む
- **Annict**: Rails CI の説明に prettier を含まない

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 6: go/CLAUDE.md - テストインフラ（TestMain パターン・bcrypt コスト低減・DB 接続プール共有）

**対象ファイル**: `go/CLAUDE.md`

**差分の内容**:

- **Wikino/Mewst**: `SetupTestMain`・`SetupTx(t)`・`GetTestDB()` パターン、bcrypt コスト低減、DB 接続プール共有の詳細セクションがある
- **Annict**: `SetupTestDB(t)` のシンプルなパターンのみ。上記セクションがない

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 7: go/CLAUDE.md - 標準ファイル名数

**対象ファイル**: `go/CLAUDE.md`

**差分の内容**:

- **Wikino**: 「10種類のみ」と記載
- **Mewst**: 「9種類のみ」と記載
- **Annict**: 記載なし

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Mewst の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 8: go/docs/architecture-guide.md - Middleware の templates 依存許可

**対象ファイル**: `go/docs/architecture-guide.md`

**差分の内容**:

- **Wikino**: Middleware はエラーページやメンテナンスページのレンダリングのため templates への依存を許可
- **Annict/Mewst**: Middleware は他の Presentation 層パッケージに依存しない

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 9: go/docs/architecture-guide.md - テストヘルパー関数名

**対象ファイル**: `go/docs/architecture-guide.md`

**差分の内容**:

- **Wikino/Mewst**: `testutil.SetupTx(t)` を使用
- **Annict**: `testutil.SetupTestDB(t)` を使用

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 10: go/docs/handler-guide.md - handler_test.go の説明

**対象ファイル**: `go/docs/handler-guide.md`

**差分の内容**:

- **Wikino/Mewst**: 「ハンドラーの統合テスト」
- **Annict**: 「ハンドラーのテスト（New, Create など各アクションのテストを含む）」

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 11: go/docs/templ-guide.md - Flash コンポーネントの ctx 引数

**対象ファイル**: `go/docs/templ-guide.md`

**差分の内容**:

- **Wikino/Annict**: `@components.Flash(flash)`
- **Mewst**: `@components.Flash(ctx, flash)`（ctx 引数が追加）

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Mewst の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 12: go/docs/templ-guide.md, validation-guide.md - テストヘルパー関数名

**対象ファイル**: `go/docs/templ-guide.md`, `go/docs/validation-guide.md`

**差分の内容**:

- **Wikino**: `testutil.SetupTx(t)` を使用
- **Annict/Mewst**: `testutil.SetupTestDB(t)` を使用（validation-guide.md のみ）

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [x] その他: これはどちらの方を採用すると良いと思いますか？

---

### カテゴリ 2: 開発環境

#### 差分 13: Dockerfile.dev - golangci-lint バージョン

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Wikino**: v2.7.2
- **Annict**: v2.6.2
- **Mewst**: v2.6.2

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 14: Dockerfile.dev - sqlc バージョン

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Wikino/Mewst**: v1.30.0
- **Annict**: v1.27.0

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 15: Dockerfile.dev - GOBIN 環境変数と /go/bin ディレクトリ作成

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Wikino/Mewst**: `GOBIN` 環境変数未設定、`/go/bin` の明示的作成なし
- **Annict**: `ENV GOBIN=/go/bin` を設定し、`mkdir -p /go/bin` で明示的にディレクトリ作成

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 16: Dockerfile.dev - mise reshim 前のコメント

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Wikino/Mewst**: shims 方式に関する詳細なコメント（3 行）がある
- **Annict**: コメントなし（`RUN mise reshim` のみ）

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 17: Dockerfile.dev - .zshrc の明示的作成

**対象ファイル**: `Dockerfile.dev`

**差分の内容**:

- **Wikino/Mewst**: `.zshrc` の明示的作成なし
- **Annict**: `RUN touch /home/${USERNAME}/.zshrc` で明示的に作成

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### カテゴリ 3: ビルド・CI 設定

#### 差分 18: go/.golangci.yml - Middleware 層の Templates 依存ルール

**対象ファイル**: `go/.golangci.yml`

**差分の内容**:

- **Wikino/Mewst**: Middleware 層から templates パッケージへの依存を許可
- **Annict**: Middleware 層から templates パッケージへの依存を禁止

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 19: go/.golangci.yml - validator-layer の glob パターン

**対象ファイル**: `go/.golangci.yml`

**差分の内容**:

- **Wikino**: `**/validator.go` を対象
- **Mewst**: `**/internal/handler/**/validator.go` を対象（より限定的）
- **Annict**: validator-layer ルール自体がない

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Mewst の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 20: go/.golangci.yml - glob パターンの書き方

**対象ファイル**: `go/.golangci.yml`

**差分の内容**:

- **Wikino**: 一部ルールで `*.go` パターン（例: `**/internal/viewmodel/*.go`）を使用
- **Annict/Mewst**: `**` パターン（例: `**/internal/viewmodel/**`）を使用

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 21: go/Makefile - tparse インストール方法

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Wikino/Mewst**: `go install github.com/mfridman/tparse`（go.mod からバージョン取得）
- **Annict**: `go install github.com/mfridman/tparse@latest`

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 22: go/Makefile - test ターゲットの tparse オプション

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Wikino**: `tparse`（オプションなし）
- **Annict/Mewst**: `tparse -all`

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 23: go/Makefile - test-verbose の実装

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Wikino/Mewst**: `tparse -all` を使用
- **Annict**: tparse を使わず `go test -v -race -count=1 ./...` を直接実行

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 24: go/Makefile - echo の絵文字使用

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Wikino**: echo に絵文字なし
- **Annict/Mewst**: `echo "🔍 golangci-lintを実行中..."` のように絵文字あり

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 25: go/Makefile - test コマンドの @ プレフィックス

**対象ファイル**: `go/Makefile`

**差分の内容**:

- **Wikino/Annict**: `@bash -c` を使用（コマンド非表示）
- **Mewst**: `bash -c`（@ なし、コマンド表示）

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Mewst の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 26: rails/Makefile - テストコマンドの環境変数設定

**対象ファイル**: `rails/Makefile`

**差分の内容**:

- **Wikino/Mewst**: `$(OP_RUN_TEST) bin/rspec` で 1Password 経由で環境変数を設定
- **Annict**: `RAILS_ENV=test bin/rspec` で 1Password 経由の設定なし

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 27: .github/workflows/go-ci.yml - CI 環境変数（CI: true）

**対象ファイル**: `.github/workflows/go-ci.yml`

**差分の内容**:

- **Wikino**: test ステップに `CI: true` 環境変数がない
- **Annict/Mewst**: test ステップに `CI: true` 環境変数がある

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 28: .github/workflows/go-ci.yml - goimports チェックの分離

**対象ファイル**: `.github/workflows/go-ci.yml`

**差分の内容**:

- **Wikino/Annict**: `Check templ generate` ステップ内で `make goimports` を実行
- **Mewst**: `Check templ generate` と `Check goimports` が別ステップ（`make goimports-check` を使用）

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Mewst の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 29: .github/workflows/rails-ci.yml - actions/setup-node バージョン

**対象ファイル**: `.github/workflows/rails-ci.yml`

**差分の内容**:

- **Wikino**: `v5` を使用
- **Annict/Mewst**: `v4` を使用

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 30: .github/workflows/rails-ci.yml - Prettier CI ジョブの有無

**対象ファイル**: `.github/workflows/rails-ci.yml`

**差分の内容**:

- **Wikino/Mewst**: Prettier の CI ジョブがある
- **Annict**: Prettier の CI ジョブがない

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 31: .github/workflows/rails-ci.yml - Capybara スクリーンショットアップロード

**対象ファイル**: `.github/workflows/rails-ci.yml`

**差分の内容**:

- **Wikino/Mewst**: テスト失敗時の Capybara スクリーンショットアップロード（`actions/upload-artifact@v4`）がある
- **Annict**: この機能がない

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 32: .github/workflows/rails-ci.yml - Capybara スクリーンショットのパス

**対象ファイル**: `.github/workflows/rails-ci.yml`

**差分の内容**:

- **Wikino**: `**/tmp/capybara/`
- **Mewst**: `rails/**/tmp/capybara/`

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Mewst の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### カテゴリ 4: ツール設定

#### 差分 33: rails/.prettierrc - Tailwind CSS プラグインと ERB オーバーライド

**対象ファイル**: `rails/.prettierrc`

**差分の内容**:

- **Wikino/Mewst**: `prettier-plugin-tailwindcss` と `@ttskch/prettier-plugin-tailwindcss-anywhere` プラグイン、`*.html.erb` への `parser: "anywhere"` オーバーライドがある
- **Annict**: これらのプラグイン・オーバーライドがない

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 34: rails/.rspec - require 対象

**対象ファイル**: `rails/.rspec`

**差分の内容**:

- **Wikino/Mewst**: `--require rails_helper`
- **Annict**: `--require spec_helper`

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [x] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 35: rails/eslint.config.mjs - 設定 API と ignore パターン

**対象ファイル**: `rails/eslint.config.mjs`

**差分の内容**:

- **Wikino/Mewst**: ESLint の新しい `defineConfig` / `globalIgnores` ヘルパーを使用、ignore は `app/assets/builds/**/*` と `tailwind.config.js` のみ
- **Annict**: フラット配列形式（旧スタイル）、ignore に `coverage/**/*`, `public/**/*`, `vendor/**/*`, `tmp/**/*`, `log/**/*`, `node_modules/**/*` も含む

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

### カテゴリ 5: Claude Code 設定

#### 差分 36: .claude/commands/commit.md - 全ファイルコミットの指示

**対象ファイル**: `.claude/commands/commit.md`

**差分の内容**:

- **Wikino/Mewst**: 「全ての未コミットファイルをコミットする」指示がある
- **Annict**: この指示がない

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 37: .claude/commands/commit.md - 変更が多岐にわたる場合の追加説明

**対象ファイル**: `.claude/commands/commit.md`

**差分の内容**:

- **Wikino/Annict**: コミット粒度の説明のみ
- **Mewst**: 「変更が多岐にわたる場合は機能ごとにコミットを作成してください」という追加説明がある

**採用方針**:

- [ ] Wikino の方式を採用（他プロジェクトに反映）
- [x] Mewst の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 38: .claude/commands/review.md - 用語（「作業計画書」vs「設計書」）

**対象ファイル**: `.claude/commands/review.md`

**差分の内容**:

- **Wikino**: 「作業計画書」という用語を使用
- **Annict/Mewst**: 「設計書」という用語を使用

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 39: .claude/commands/review.md - レビュードキュメントのファイル命名規則

**対象ファイル**: `.claude/commands/review.md`

**差分の内容**:

- **Wikino**: `docs/reviews/{ブランチ名}.md`、同名ファイル存在時は末尾に連番（例: `rate-limit-1.md`）
- **Annict/Mewst**: `docs/reviews/{ブランチ名}-{YYYYMMDDHHMM}.md`、日時タイムスタンプをサフィックスに付与

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

#### 差分 40: .claude/commands/sync.md - ファイル名と比較対象範囲

**対象ファイル**: `.claude/commands/sync.md`

**差分の内容**:

- **Wikino/Mewst**: `sync.md` として存在。5 カテゴリに分類して管理（ガイドライン、開発環境、ビルド・CI、ツール設定、Claude Code 設定）。スキップ済み差分の管理機構あり
- **Annict**: `sync-guideline.md` として存在。CLAUDE.md 群と Go 版ガイドラインのみ。カテゴリ分けなし。スキップ済み差分の管理機構なし

**採用方針**:

- [x] Wikino の方式を採用（他プロジェクトに反映）
- [ ] Annict の方式を採用（他プロジェクトに反映）
- [ ] スキップ（対応不要）
- [ ] その他: <!-- 記入 -->

---

## タスクリスト

### フェーズ 1: 採用方針の決定

- [x] **1-1**: 差分レポートを確認し、各差分の採用方針を記入する
  - 各差分の「採用方針」チェックボックスにチェックを入れる
  - 必要に応じて「その他」に補足を記入する
  - **担当**: ユーザー

### フェーズ 2: タスクリストの作成

- [x] **2-1**: 採用方針を元に自プロジェクトのタスクリストを追加する
  - ユーザーが記入した採用方針に基づき、フェーズ 3 以降に自プロジェクトの具体的なタスクを追加する
  - **重要**: 「スキップ（対応不要）」が選ばれた差分は `.claude/commands/sync.md` の「スキップ済みの差分」セクションに追記するタスクも含めること
  - 他プロジェクトへの反映は、各プロジェクトで `/sync` を実行して対応する
  - **担当**: Claude

### フェーズ 3: CLAUDE.md の更新（差分 1, 2, 3）

- [x] **3-1**: `CLAUDE.md` にセッションストアセクションを追加（差分 1）
  - `## 共通インフラ` 内の `### データベース（PostgreSQL）` の後に `### セッションストア（PostgreSQL）` セクションを追加
  - Annict の内容を Wikino 用に調整して記載
- [x] **3-2**: `CLAUDE.md` の CI/CD 設定ファイルの記載を更新（差分 2）
  - `lint-and-test.yml` → `go-ci.yml` + `rails-ci.yml` に変更
- [x] **3-3**: `CLAUDE.md` のレビューガイドライン内の用語を更新（差分 3）
  - 「リクエストバリデーション」→「バリデーション方針」
  - 「ビューモデル・ユースケース・ワーカー」→「ビューモデル・ユースケース」
  - 「Usecase、Worker、Repositoryの使い分け」→「Usecase、Repositoryの使い分け」
  - 「Request DTOパターン」→「バリデーションの分類」
  - 「バリデーション範囲」→「状態バリデーションの配置基準」

### フェーズ 4: go/CLAUDE.md の更新（差分 7, 12）

- [x] **4-1**: `go/CLAUDE.md` の標準ファイル名数を修正（差分 7）
  - 「標準ファイル名（10 種類のみ）」→「標準ファイル名（9 種類のみ）」
- [x] **4-2**: 差分 12 は Wikino 方式（`SetupTx(t)`）を維持（対応不要）
  - 差分 6 で Wikino 方式を採用したため、一貫性を保つ

### フェーズ 5: go/.golangci.yml の更新（差分 19, 20）

- [x] **5-1**: validator-layer の glob パターンを限定的に変更（差分 19）
  - `"**/validator.go"` → `"**/internal/handler/**/validator.go"`
- [x] **5-2**: glob パターンの書き方を `**` スタイルに統一（差分 20）
  - `"**/internal/viewmodel/*.go"` → `"**/internal/viewmodel/**"` 等

### フェーズ 6: go/Makefile の更新（差分 22, 24）

- [x] **6-1**: test ターゲットの tparse オプションを追加（差分 22）
  - `tparse` → `tparse -all`
- [x] **6-2**: lint-golangci の echo に絵文字を追加（差分 24）
  - `@echo "golangci-lintを実行中..."` → `@echo "🔍 golangci-lintを実行中..."`

### フェーズ 7: CI 設定・Claude Code 設定の更新（差分 32, 37）

- [x] **7-1**: rails-ci.yml の Capybara スクリーンショットパスを修正（差分 32）
  - `"**/tmp/capybara/"` → `"rails/**/tmp/capybara/"`
- [x] **7-2**: commit.md に複数スコープのコミット説明を追加（差分 37）
  - 分割の基準セクション後に「変更が多岐にわたる場合は機能ごとにコミットを作成してください」を追加

### フェーズ 8: スキップ済み差分の更新

- [x] **8-1**: `.claude/commands/sync.md` の「スキップ済みの差分」に差分 34 を追記
  - `rails/.rspec`: require 対象の違い（プロジェクト固有のため）
- [x] **8-2**: `.claude/commands/sync.md` に採用方針の推奨ガイダンスを追加
