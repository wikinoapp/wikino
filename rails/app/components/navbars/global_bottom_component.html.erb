<nav class="md:hidden" aria-label="<%= t("messages.navbars.global_bottom_label") %>">
  <%= render Navbars::GlobalMenuComponent.new(
    current_page_name:,
    current_user:,
    class_name: "bg-card w-fit rounded-full border border-stone-400 px-4 py-1"
  ) %>
</nav>
