<nav
  class="fixed left-2 top-1/2 z-sticky-bar hidden -translate-y-1/2 md:flex"
  aria-label="<%= t("messages.navbars.global_rail_label") %>"
>
  <%= render Navbars::GlobalMenuComponent.new(
    current_page_name:,
    current_user:,
    current_space:,
    class_name: "bg-card flex-col rounded-full border border-stone-400 px-1 py-4"
  ) %>
</nav>
