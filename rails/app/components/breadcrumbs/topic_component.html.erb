<%= render Breadcrumbs::SpaceComponent.new(signed_in: signed_in?, space:) do |breadcrumb| %>
  <%= breadcrumb.with_item do %>
    <%= link_to(
      topic_path(space.identifier, topic.number),
      class: "flex gap-0.5 items-center"
    ) do %>
      <%= render Icons::TopicComponent.new(topic:, size: "16px", class_name: "fill-gray-600") %>

      <%= topic.name %>
    <% end %>
  <% end %>

  <% items.each do |item| %>
    <%= breadcrumb.with_item do %>
      <%= item %>
    <% end %>
  <% end %>
<% end %>
