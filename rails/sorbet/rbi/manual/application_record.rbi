# typed: strict
# frozen_string_literal: true

class ApplicationRecord
  def self.has_one_attached(*args)
  end

  def to_gid_param(options = T.unsafe(nil))
  end

  def to_global_id(options = T.unsafe(nil))
  end
end
